name: Pr validation

on: 
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TOKEN: "ola"
    steps:
    - name: steps1
      uses: actions/checkout@v4
      with:
        path: ./download
    - name: step2
      run: env    
    - name: step3 
      run: |
        ls -lthr ./download
        echo "${{ secrets.VERSION }}"
        echo "${{ env.TOKEN }}"
    - name: step 2
      run: echo "hellow"
        needs: [steps1,step2,step3]
    - name: Python Syntax Checker
      uses: cclauss/Find-Python-syntax-errors-action@v0.2.0
